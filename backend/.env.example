# Backend environment
# Copy to .env and adjust values

# Server
PORT=5000

# MySQL (host/port style)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=yourpassword
DB_NAME=drugstore

# Alternative: single connection URL
# DATABASE_URL=mysql://user:password@host:3306/dbname
